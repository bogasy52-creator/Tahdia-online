{
  "name": "busraj-games",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test test/*.test.js",
    "verify": "npm test && node scripts/verify-static.mjs && npm run check"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
