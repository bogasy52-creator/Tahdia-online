name: Auto Update Game

on:
  push:
    branches:
      - main
    paths:
      - "**.zip"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Download files
        uses: actions/checkout@v4

      - name: Unzip game update
        run: |
          GAME="tahadi-alabaqera-online-pro-fixed"

          ZIP=$(find . -name "*.zip" | head -1)

          if [ -z "$ZIP" ]; then
            echo "No ZIP file found"
            exit 1
          fi

          echo "Updating game..."

          unzip -o "$ZIP" -d "$GAME"

          rm -f "$ZIP"

      - name: Save update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add .

          git commit -m "Auto update game" || echo "No changes"

          git push
          rm -f "$ZIP"

          echo "Game updated successfully"

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add .

          git commit -m "Auto update game files" || echo "No changes"

          git push
